/* 
  This file was generated automatically with ../../scripts/math2c.pl.
  Do not edit this file directly as it can be overwritten!!

  Mathematica source: ../../mathematica/functionals/gga_x_rpbe.m
  Type of functional: work_gga_x
*/

void XC(math2c_gga_x_rpbe_enhance)
  (const XC(func_type) *p, int order, 
   FLOAT x, FLOAT *f, FLOAT *dfdx, FLOAT *d2fdx2, FLOAT *d3fdx3)
{
  gga_x_rpbe_params *params;
 
  assert(p->params != NULL);
  params = (gga_x_rpbe_params * )(p->params);


  if(order < 0) return;

  *f = 1. + (1. - EXP(-(params->mu*POW(x,2)*POW(X2S,2))/params->kappa))*params->kappa;

  if(order < 1) return;

  *dfdx = (2*params->mu*x*POW(X2S,2))/EXP((params->mu*POW(x,2)*POW(X2S,2))/params->kappa);

  if(order < 2) return;

  *d2fdx2 = (2*params->mu*POW(X2S,2)*(params->kappa - 2*params->mu*POW(x,2)*POW(X2S,2)))/(EXP((params->mu*POW(x,2)*POW(X2S,2))/params->kappa)*params->kappa);

  if(order < 3) return;

  *d3fdx3 = (4*POW(params->mu,2)*x*POW(X2S,4)*(-3*params->kappa + 2*params->mu*POW(x,2)*POW(X2S,2)))/(EXP((params->mu*POW(x,2)*POW(X2S,2))/params->kappa)*POW(params->kappa,2));
}


#define math2c_order 3
#define math2c_func  XC(math2c_gga_x_rpbe_enhance)

