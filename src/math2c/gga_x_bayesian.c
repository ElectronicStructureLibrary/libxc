/* 
  This file was generated automatically with ./scripts/math2c.pl.
  Do not edit this file directly as it can be overwritten!!

  Mathematica source: ./mathematica/functionals/gga_x_bayesian.m
  Type of functional: work_gga_x
*/

void XC(math2c_gga_x_bayesian_enhance)
  (const XC(func_type) *p, int order, 
   FLOAT x, FLOAT *f, FLOAT *dfdx, FLOAT *d2fdx2, FLOAT *d3fdx3)
{


  if(order < 0) return;

  *f = theta0 + (theta2*POW(x,4)*POW(X2S,4))/POW(1. + x*X2S,4) + (theta1*POW(x,2)*POW(X2S,2))/POW(1. + x*X2S,2);

  if(order < 1) return;

  *dfdx = (x*POW(X2S,2)*(4.*theta2*POW(x,2)*POW(X2S,2) + theta1*(2. + 4.*x*X2S + 2.*POW(x,2)*POW(X2S,2))))/POW(1. + x*X2S,5);

  if(order < 2) return;

  *d2fdx2 = (theta2*POW(x,2)*POW(X2S,4)*(12. - 8.*x*X2S) + theta1*(2.*POW(X2S,2) - 6.*POW(x,2)*POW(X2S,4) - 4.*POW(x,3)*POW(X2S,5)))/POW(1. + x*X2S,6);

  if(order < 3) return;

  *d3fdx3 = (POW(X2S,3)*(theta2*x*X2S*(24. - 72.*x*X2S + 24.*POW(x,2)*POW(X2S,2)) + theta1*(-12. - 12.*x*X2S + 12.*POW(x,2)*POW(X2S,2) + 12.*POW(x,3)*POW(X2S,3))))/POW(1. + x*X2S,7);
}


#define math2c_order 3
#define math2c_func  XC(math2c_gga_x_bayesian_enhance)

