/* 
  This file was generated automatically with scripts/math2c.pl.
  Do not edit this file directly as it can be overwritten!!

  Mathematica source: ./mathematica/functionals/gga_x_ak13.m
  Type of functional: work_gga_x
*/

void XC(math2c_gga_x_ak13_enhance)
  (const XC(func_type) *p, int order, 
   FLOAT x, FLOAT *f, FLOAT *dfdx, FLOAT *d2fdx2, FLOAT *d3fdx3)
{


  if(order < 0) return;

  *f = 1. + B1*x*X2S*LOG(1. + x*X2S) + B2*x*X2S*LOG(1. + LOG(1. + x*X2S));

  if(order < 1) return;

  *dfdx = X2S*(B1*LOG(1. + x*X2S) + (x*X2S*(B1 + B2/(1. + LOG(1. + x*X2S))))/(1. + x*X2S) + B2*LOG(1. + LOG(1. + x*X2S)));

  if(order < 2) return;

  *d2fdx2 = (POW(X2S,2)*(2.*B2 + B1*(2. + 1.*x*X2S) + (B2*(2. + x*X2S) + B1*(4. + 2.*x*X2S))*LOG(1. + x*X2S) + B1*(2. + x*X2S)*POW(LOG(1. + x*X2S),2)))/(POW(1. + x*X2S,2)*POW(1. + LOG(1. + x*X2S),2));

  if(order < 3) return;

  *d3fdx3 = (POW(X2S,3)*(B1*(-3. - 1.*x*X2S) + B2*(-6. + 1.*x*X2S) + (B1*(-9. - 3.*x*X2S) + B2*(-9. - 2.*x*X2S))*LOG(1. + x*X2S) + (B1*(-9. - 3.*x*X2S) + B2*(-3. - 1.*x*X2S))*POW(LOG(1. + x*X2S),2) + B1*(-3. - 1.*x*X2S)*POW(LOG(1. + x*X2S),3)))/(POW(1. + x*X2S,3)*POW(1. + LOG(1. + x*X2S),3));
}


#define math2c_order 3
#define math2c_func  XC(math2c_gga_x_ak13_enhance)

